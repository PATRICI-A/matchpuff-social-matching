server:
  port: ${SERVER_PORT:8080}
  ssl:
    enabled: false

app:
  startup:
    fail-fast: false

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
